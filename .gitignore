node_modules/

# Agent Kit
.agent/

# Environment files (never commit secrets!)
.env
.env.local
.env.*.local
.env.*.development
.env.*.production
# ensure production env files are never tracked
.env.production

# Backups do banco (dump local, fora do git)
backups/
backup/backup.log

# Build output
frontend/dist/
frontend/tsconfig.tsbuildinfo
